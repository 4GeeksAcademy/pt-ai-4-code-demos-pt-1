# Rule: Commit Frequently

- After each completed step (and sometimes mid-step for large changes), create a Git commit with a descriptive message referencing the plan step.
- Do not bundle unrelated changes into a single commit.
- Commit messages should follow the format: `refactor(step): short description` — e.g., `refactor(01): consolidate tag generation into single module`.