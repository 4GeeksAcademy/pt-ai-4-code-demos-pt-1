# Rule: Document Every Change

- Every code change must be accompanied by an update to the relevant plan step status and/or a decision record, unless the change is purely mechanical (e.g., renaming a file that was already marked for rename in the plan).
- After completing a step, the agent must update the refactor plan index to reflect completion.