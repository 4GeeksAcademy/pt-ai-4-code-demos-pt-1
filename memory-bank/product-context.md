# Note-Taking App Product Context

Note-Taking App is a single-page app for taking and organizing notes.  It will store the notes using the `localStorage` api.

## Project Architecture

- React on `vite` and written in typescript.
- TailwindCSS 4 for styling
